sudo ifconfig swp1 0.0.0.0 up
sudo ifconfig swp2 0.0.0.0 up
echo sudo ip link add name br0 type bridg
echo sudo ip link set dev br0 up
echo sudo ip link set dev swp1 master br0
echo sudo ip link set dev swp2 master br0
