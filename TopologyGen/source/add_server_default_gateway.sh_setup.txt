sudo ifconfig up
sudo ifconfig grep | up
sudo ifconfig in eachServer up
sudo ifconfig up
sudo ifconfig ip sudo up
sudo ifconfig | $eachServer up
sudo ifconfig | $ip up
sudo ifconfig ip sudo up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig { up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig "${1}" [ up
sudo ifconfig "${@}" up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig grep | up
sudo ifconfig in eachServer up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig grep | up
sudo ifconfig in eachServer up
sudo ifconfig up
sudo ifconfig ip sudo up
