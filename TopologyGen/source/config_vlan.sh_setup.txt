sudo ifconfig up
sudo ifconfig ${YELLOW}"Adding -e up
sudo ifconfig COM SWITCHS up
sudo ifconfig grep | up
sudo ifconfig in eachSwitch up
sudo ifconfig up
sudo ifconfig ip sudo up
sudo ifconfig allInterfaces -a up
sudo ifconfig up
sudo ifconfig i=0; (( up
sudo ifconfig ip sudo up
sudo ifconfig ip sudo up
sudo ifconfig ip sudo up
sudo ifconfig ip sudo up
sudo ifconfig ip sudo up
sudo ifconfig up
sudo ifconfig | $eachSwitch up
sudo ifconfig | $interfaces up
sudo ifconfig allInterfaces -a up
sudo ifconfig up
sudo ifconfig da #numero up
sudo ifconfig i=0; (( up
sudo ifconfig ip sudo up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig i=0; (( up
sudo ifconfig ip sudo up
sudo ifconfig up
