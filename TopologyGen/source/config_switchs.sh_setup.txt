sudo ifconfig up
sudo ifconfig $createSwitchs [[ up
sudo ifconfig ${YELLOW}"Setting -e up
sudo ifconfig grep | up
sudo ifconfig in eachSwitch up
sudo ifconfig up
sudo ifconfig ip sudo up
sudo ifconfig ip sudo up
sudo ifconfig | $eachSwitch up
sudo ifconfig in eachInterface up
sudo ifconfig ip sudo up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig { up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig up
sudo ifconfig "${1}" [ up
sudo ifconfig "${@}" up
sudo ifconfig up
