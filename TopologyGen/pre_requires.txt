python 2.7
bash 5

pip install

treelib
pydotplus
bcolors
jinja2


instalar vagrant 2.2.9 .deb pelo terminal da WSL e pelo msi no windows
sudo apt-get install libxslt-dev libxml2-dev libvirt-dev zlib1g-dev ruby-dev
sudo apt-get install virt-manager

vagrant plugin install vagrant-mutate
vagrant plugin install vagrant-libvirt


instalar no windows o vagrant da mesma versão que WSL
baixar VirtualBox
export VAGRANT_WSL_ENABLE_WINDOWS_ACCESS="1"
export PATH="$PATH:/mnt/c/Program Files/Oracle/VirtualBox"

para achar o arquivo .vagrant.d
export VAGRANT_WSL_WINDOWS_ACCESS_USER_HOME_PATH="/home/matferronato/"

vagrant precisa ser criado em uma pasta do windows

hashicorp/bionic64
